import {UpdateComponent} from "../../components/update/UpdateComponent.mjs";

const UpdateLayout = new LayoutLoader(createLayout(
    [
        [UpdateComponent]
    ]
));

export {UpdateLayout}